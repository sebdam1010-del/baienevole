# Server configuration
PORT=3000
NODE_ENV=development

# Database configuration (SQLite - fichier local)
DATABASE_URL="file:./prisma/dev.db"

# JWT configuration
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production
JWT_EXPIRES_IN=24h

# Email configuration (SMTP)
# En développement, laisser vide pour utiliser Ethereal Email (emails de test)
# En production, configurer avec votre serveur SMTP
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your_email@example.com
SMTP_PASS=your_smtp_password
SMTP_FROM="La Baie des Singes <noreply@baiedessinges.com>"

# Frontend URL (pour CORS)
FRONTEND_URL=http://localhost:5173
